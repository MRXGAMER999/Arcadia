package com.example.arcadia.presentation.screens.roast

/**
 * Timing constants for the roast reveal animation sequence.
 * 
 * Requirements: 4.2, 4.3, 4.4, 4.5, 4.6
 */
object RevealTiming {
    const val TITLE_DELAY_MS = 0L
    const val HEADLINE_DELAY_MS = 300L
    const val COULD_HAVE_DELAY_MS = 600L
    const val COULD_HAVE_ITEM_STAGGER_MS = 150L
    const val PREDICTION_DELAY_MS = 1200L
    const val WHOLESOME_DELAY_MS = 1800L
    const val BUTTONS_DELAY_MS = 2200L
    
    const val TYPEWRITER_CHAR_DELAY_MS = 30L
    const val MESSAGE_ROTATION_INTERVAL_MS = 2500L
}
